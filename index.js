import {callZipApi} from './zipApi'

const latlngtest = (options) => {
    //options.address = passed address

    //check if it has Zip/City+State
        //Call ZipAPI
    //Else
        //Call SmartyStreets USStreetAPI
            //Error(Call Google Geocoder)
}

module.exports.latlngtest = latlngtest;